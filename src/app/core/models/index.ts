export interface Person {
  gender: string;
  lastName: string;
  firstName: string;
  email: string;
  phone: string;
}

export interface Project {
  ownership: string;
  peopleNumber: number;
  income: number;
  surface: number;
}
